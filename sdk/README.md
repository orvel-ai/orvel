# `@orvel/sdk`

The public developer-facing entry point for Orvel. It currently re-exports the
initial contracts from the focused packages and the minimal runtime constructor.

The SDK is private and unpublished while these APIs are being established.
