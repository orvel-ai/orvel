# `@orvel/sdk`

The public developer-facing entry point for Orvel. `createOrvelClient` composes
an injected store, runtime, and teaching retriever into agent, conversation,
teaching, and eval APIs without any dependency on Next.js or Studio.

The SDK is private and unpublished while these APIs are being established.
