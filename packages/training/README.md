# `@orvel/training`

Structured correction records, deterministic retrieval, and explicit teaching
context for v0.1. It stores the original input and response alongside a
creator-corrected response, then selects only relevant examples for a future
question.

It does not perform optimization, fine-tuning, or preference learning.
