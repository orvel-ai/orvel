# `@orvel/openai`

The v0.1 OpenAI adapter. It implements the provider-neutral runtime contract
using the Responses API and receives its key from the server environment. It is
not imported by `@orvel/runtime`.
