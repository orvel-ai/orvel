# `@orvel/runtime`

The core execution layer for Orvel agents. It owns provider-neutral model
contracts and selects a registered model provider for each run.

The package does not contain a hosted-provider adapter, persistence, a tool loop,
or deployment behavior yet.
